# TEST_SPEC.md — taskq Test Specification Catalog

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` skill protocol. Test names from P1 `TEST_INVENTORY.yaml`
> are preserved as-is where specified. This is the SINGLE SOURCE OF TRUTH for
> all test traceability checks at Gates 1-4. Validated by Agent B (TECH_LEAD)
> before P2 exit. P3 Agent A implements tests FROM this catalog — not ad-hoc.

**Project**: taskq
**SRS version**: `01-requirements/SRS.md` — canonical source `SPEC.md` v4.0.0 (2026-07-11)
**SAD version**: `02-architecture/SAD.md` — SAB `created_at: 2026-07-24`
**TEST_INVENTORY.yaml version**: `format_version: "2.0"` (34 counted test contracts, `01-requirements/TRACEABILITY_MATRIX.md` v1.0.0)
**Generated**: 2026-07-24
**Active NFR Patterns**: NP-02, NP-03, NP-04, NP-06, NP-07, NP-08, NP-09, NP-11, NP-13, NP-14, NP-15

---

## NFR Pattern Activation (Step 1 + Step 1b + Step 1c Output)

<!-- All 15 patterns enumerated per derive_test_cases.md Step 1. "SRS: <keyword>"
     = Step 1 keyword scan; "SAD: <module>" = Step 1b architecture-risk trigger
     (mandatory regardless of SRS keywords); "SEC: <threat-id>" = Step 1c SAD §6
     STRIDE threat trigger (mandatory regardless of SRS keywords). -->

| Pattern | Trigger (SRS keyword / SAD module / SEC threat) | Activated |
|---------|--------------------------------------------------|-----------|
| NP-01 (auth 401)          | not found — taskq has no authentication surface                       | ❌ |
| NP-02 (authz 403)         | SEC: T-07 (elevation_of_privilege forces NP-02, owner `taskq.cli`)     | ✅ |
| NP-03 (rate limit 429)    | SEC: T-05 (denial_of_service forces NP-03, owner `taskq.store`)       | ✅ |
| NP-04 (validation 422)    | SRS: "validation" (AC-1.1..1.4, NFR-02) + SEC: T-01/T-02/T-04 (tampering forces NP-04+NP-08) | ✅ |
| NP-05 (idempotency)       | not found — no "idempotent"/"at-least-once" keyword in SRS            | ❌ |
| NP-06 (latency SLA)       | SRS: "p95" (NFR-01, NFR-09)                                            | ✅ |
| NP-07 (dependency fault)  | SRS: "resilience"/"fault" (NFR-07) + SAD: `taskq.cache` (cache/optional-dependency trait) | ✅ |
| NP-08 (security attack)   | SRS: "injection" (NFR-02) + SEC: T-01/T-02/T-03/T-04                   | ✅ |
| NP-09 (audit log)         | SEC: T-06 (repudiation forces NP-09, owner `taskq.store`) — no SRS keyword alone | ✅ |
| NP-10 (data round-trip)   | SRS uses "persist" repeatedly but no dedicated round-trip case authored — see Direction B Properties tables (FR-03, FR-04) instead | ❌ |
| NP-11 (backward compat)   | SRS: "version"/"migration" (NFR-10)                                    | ✅ |
| NP-12 (pagination)        | not found — `list` has no pagination in SPEC.md §3 FR-05               | ❌ |
| NP-13 (concurrency)       | SRS: "concurrency"/"thread" (FR-02, NFR-08) + SAD: `taskq.store` (shared mutable state + threads) | ✅ |
| NP-14 (encryption)        | SEC: T-03 (information_disclosure forces NP-08+NP-14, owner `taskq.executor`) — no SRS keyword alone | ✅ |
| NP-15 (timeout)           | SRS: "timeout" (FR-02) + SAD: `taskq.executor` (external process/subprocess)          | ✅ |

---

## Functional Requirement Test Cases

### FR-01: Task submission and validation

**Classification**: DATA_ENTITY, SECURITY_CONTROL
**Active Patterns**: NP-04, NP-08, NP-03, NP-09

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_submit_empty_command_exits_2` | command=""; expected_exit="2" | validation | Q2/AC-1.1 |
| 2 | `test_submit_long_command_exits_2` | command_length="1001"; expected_exit="2" | boundary | Q3/AC-1.2 |
| 3 | `test_submit_command_at_1000_chars_accepted` | command_length="1000"; expected_exit="0" | boundary | Q3/AC-1.2 |
| 4 | `test_submit_injection_semicolon_rejected` | char_name="semicolon"; command="echo hi; rm x"; expected_exit="2" | validation | Q2/AC-1.3 |
| 5 | `test_submit_injection_pipe_rejected` | char_name="pipe"; command="echo hi \| wc"; expected_exit="2" | validation | Q2/AC-1.3 |
| 6 | `test_submit_injection_ampersand_rejected` | char_name="ampersand"; command="echo hi & sleep 1"; expected_exit="2" | validation | Q2/AC-1.3 |
| 7 | `test_submit_injection_dollar_rejected` | char_name="dollar"; command="echo $HOME"; expected_exit="2" | validation | Q2/AC-1.3 |
| 8 | `test_submit_injection_greater_than_rejected` | char_name="greater_than"; command="echo hi > out.txt"; expected_exit="2" | validation | Q2/AC-1.3 |
| 9 | `test_submit_injection_less_than_rejected` | char_name="less_than"; command="echo hi < in.txt"; expected_exit="2" | validation | Q2/AC-1.3 |
| 10 | `test_submit_injection_backtick_rejected` | char_name="backtick"; command="echo `whoami`"; expected_exit="2" | validation | Q2/AC-1.3 |
| 11 | `test_name_uniqueness` | name="build"; existing_status="pending"; expected_exit="2" | validation | Q2/AC-1.4 |
| 12 | `test_atomic_add_task` | command="echo hi"; expected_status="pending" | happy_path | Q1/AC-1.5 |
| 13 | `test_submit_json_output` | command="echo hi"; json_flag="true"; expected_status="pending" | happy_path | Q1/AC-1.6 |
| 14 | `test_submit_rejects_injection_chars` | chars_list="semicolon;pipe;ampersand;dollar;greater_than;less_than;backtick"; expected_exit="2" | nfr_pattern | Q6/1c/NP-08,NP-04 (SEC:T-01) |
| 15 | `test_cross_process_no_corruption` | process_count="4"; expected_valid_json="true" | nfr_pattern | Q6/1c/NP-08,NP-04 (SEC:T-04) — tests/integration/ |
| 16 | `test_fault_injection_fails_fast_or_recovers` | fault_scenario="kill-mid-write"; expected_outcome="fail_fast_or_recover" | nfr_pattern | Q6/1c/NP-03 (SEC:T-05) — tests/integration/ |
| 17 | `test_task_records_timestamps` | task_id="a1b2c3d4"; expected_fields="created_at,finished_at,exit_code,status" | nfr_pattern | Q6/1c/NP-09 (SEC:T-06) |
| 18 | `test_name_uniqueness` | name="build"; existing_status="running"; expected_exit="2" | validation | Q2/AC-1.4 |

**Sub-assertions** (predicate over a case's declared Inputs / the production `result`):

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC1-empty-stripped | `command.strip() == ""` | 1 |
| AC1-boundary-long | `command_length == "1001"` | 2 |
| AC1-boundary-at | `command_length == "1000"` | 3 |
| AC1-validation-exit2 | `expected_exit == "2"` | 1, 4, 5, 6, 7, 8, 9, 10, 11 |
| AC1-name-existing-state | `existing_status == "pending"` | 11 |
| AC1-name-existing-state-running | `existing_status == "running"` | 18 |
| AC1-happy-atomic | `command == "echo hi"` | 12 |
| AC1-happy-json | `json_flag == "true"` | 13 |
| AC1-aggregate-injection | `"semicolon" in chars_list` | 14 |
| AC1-cross-process-valid | `expected_valid_json == "true"` | 15 |
| AC1-fault-outcome | `expected_outcome == "fail_fast_or_recover"` | 16 |
| AC1-timestamp-fields | `"created_at" in expected_fields` | 17 |

---

### FR-02: Task executor

**Classification**: STATE_MACHINE, INTEGRATION
**Active Patterns**: NP-13, NP-15, NP-08, NP-14

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_shell_true_grep_zero_matches` | command="echo hi"; shell_flag="false" | negative_constraint | Q8/AC-2.1 |
| 2 | `test_state_transitions` | command="true"; exit_code="0"; expected_status="done" | happy_path | Q1/AC-2.2 |
| 3 | `test_state_transitions` | command="false"; exit_code="1"; expected_status="failed" | validation | Q2/AC-2.2 |
| 4 | `test_state_transitions` | command="sleep 10"; timeout_flag="true"; expected_status="timeout" | state_transition | Q4/AC-2.2 |
| 5 | `test_result_record_fields` | stdout_len="2500"; expected_tail_len="2000" | boundary | Q3/AC-2.3 |
| 6 | `test_run_all_concurrent` | task_count="10"; max_workers="4" | nfr_pattern | Q6/NP-13/AC-2.4 |
| 7 | `test_timeout_exit_code_4` | command="sleep 10"; timeout_seconds="1"; expected_exit="4" | validation | Q2/AC-2.5 |
| 8 | `test_run_all_concurrent_state_transitions_isolated` | task_count="20"; max_workers="4"; expected_loss="0" | nfr_pattern | Q6/1b/NP-13 (SAD: taskq.store) — tests/integration/ |
| 9 | `test_run_subprocess_orphan_cleanup_on_timeout` | command="sleep 10"; timeout_seconds="1"; expected_process_state="terminated" | nfr_pattern | Q6/1b/NP-15 (SAD: taskq.executor) — tests/integration/ |
| 10 | `test_no_shell_true_in_source` | source_scan="production"; shell_flag="false" | nfr_pattern | Q6/1c/NP-08,NP-04 (SEC:T-02) |
| 11 | `test_sk_token_redacted` | stdout_line="sk-abcdefgh12345678EXTRA"; stream="stdout"; expected_redacted="true" | nfr_pattern | Q6/1c/NP-14,NP-08 (SEC:T-03) |
| 12 | `test_token_equals_redacted` | stdout_line="token=abc123XYZ"; stream="stdout"; expected_redacted="true" | nfr_pattern | Q6/1c/NP-14,NP-08 (SEC:T-03) |
| 13 | `test_sk_token_redacted_stderr` | stderr_line="sk-abcdefgh12345678EXTRA"; stream="stderr"; expected_redacted="true" | nfr_pattern | Q6/1c/NP-14,NP-08 (SEC:T-03) |
| 14 | `test_token_equals_redacted_stderr` | stderr_line="token=abc123XYZ"; stream="stderr"; expected_redacted="true" | nfr_pattern | Q6/1c/NP-14,NP-08 (SEC:T-03) |
| 15 | `test_secret_redaction_before_truncation` | stdout_len="2010"; secret_start_offset="1990"; secret_pattern="sk-*"; expected_redacted="true"; expected_tail_len="2000" | boundary | Q3/1c/NP-14,NP-08 (SEC:T-03) — boundary counterexample, redact precedes truncate |
| 16 | `test_fr02_output_feeds_fr03_retry_pipeline` | task_status="failed"; retry_eligible="true" | integration | Q7/FR-03 |

**Sub-assertions** (predicate over a case's declared Inputs / the production `result`):

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC21-no-shell | `shell_flag == "false"` | 1, 10 |
| AC22-done-exit0 | `exit_code == "0"` | 2 |
| AC22-failed-exit1 | `exit_code == "1"` | 3 |
| AC22-timeout-flag | `timeout_flag == "true"` | 4 |
| AC23-tail-truncated | `stdout_len > expected_tail_len` | 5 |
| AC24-worker-count | `max_workers == "4"` | 6, 8 |
| AC25-exit4 | `expected_exit == "4"` | 7 |
| AC26-concurrent-no-loss | `expected_loss == "0"` | 8 |
| AC27-orphan-terminated | `expected_process_state == "terminated"` | 9 |
| AC28-redaction | `expected_redacted == "true"` | 11, 12, 13, 14, 15 |
| AC28-stream-stdout | `stream == "stdout"` | 11, 12 |
| AC28-stream-stderr | `stream == "stderr"` | 13, 14 |
| AC28-boundary-tail | `expected_tail_len == "2000"` | 15 |
| AC29-retry-eligible | `retry_eligible == "true"` | 16 |

---

### FR-03: Retry and circuit breaker

**Classification**: STATE_MACHINE
**Active Patterns**: NP-07, NP-15

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_retry_exponential_backoff` | attempt_n="1"; backoff_base="1"; expected_delay="2"; state_mode="isolate_per_test" | state_transition | Q4/AC-3.1 |
| 2 | `test_retry_exponential_backoff` | attempt_n="2"; backoff_base="1"; expected_delay="4"; state_mode="isolate_per_test" | state_transition | Q4/AC-3.1 |
| 3 | `test_breaker_open_threshold` | consecutive_failures="2"; threshold="3"; expected_breaker_state="CLOSED"; state_mode="isolate_per_test" | state_transition | Q4/AC-3.2 |
| 4 | `test_breaker_open_threshold` | consecutive_failures="3"; threshold="3"; expected_breaker_state="OPEN"; state_mode="isolate_per_test" | state_transition | Q4/AC-3.2 |
| 5 | `test_breaker_open_refuses_exit_3` | breaker_state="OPEN"; expected_exit="3"; state_mode="isolate_per_test" | validation | Q2/AC-3.3 |
| 6 | `test_breaker_half_open_probe` | breaker_state="HALF_OPEN"; probe_result="success"; expected_next_state="CLOSED"; state_mode="isolate_per_test" | state_transition | Q4/AC-3.4 |
| 7 | `test_breaker_half_open_probe` | breaker_state="HALF_OPEN"; probe_result="failure"; expected_next_state="OPEN"; state_mode="isolate_per_test" | state_transition | Q4/AC-3.4 |
| 8 | `test_breaker_state_persistence` | breaker_state="OPEN"; failure_count="3"; signature="abc123def456"; result="done"; state_mode="isolate_per_test" | happy_path | Q1/AC-3.5 |
| 9 | `test_fr03_retry_backoff_bounded` | retry_limit="3"; max_workers="4"; expected_max_concurrent_retries="12" | nfr_pattern | Q6/1b/NP-07,NP-15 (SAD: taskq.executor retry-logic trait) — tests/integration/ |

**Sub-assertions** (predicate over a case's declared Inputs / the production `result`):

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC31-delay-n1 | `expected_delay == "2"` | 1 |
| AC31-delay-n2 | `expected_delay == "4"` | 2 |
| AC32-closed | `expected_breaker_state == "CLOSED"` | 3 |
| AC32-open | `expected_breaker_state == "OPEN"` | 4 |
| AC33-exit3 | `expected_exit == "3"` | 5 |
| AC34-half-success | `expected_next_state == "CLOSED"` | 6 |
| AC34-half-failure | `expected_next_state == "OPEN"` | 7 |
| AC35-persist-count | `failure_count == "3"` | 8 |
| AC36-backoff-bound | `expected_max_concurrent_retries == "12"` | 9 |

**Properties** — round-trip invariant on breaker persistence (Direction B):

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| P3-breaker-roundtrip | `load_breaker(save_breaker(breaker_state)) == breaker_state` | 8 |

`load_breaker`/`save_breaker` are symbolic (not bound to a declared Input), so the
P2 engine cannot evaluate this invariant against case 8's concrete inputs and
correctly degrades it to `needs_review` — the P4 `hypothesis @given` test is
what actually exercises the round trip across arbitrary breaker states.

---

### FR-04: Result TTL cache

**Classification**: ALGORITHM, INTEGRATION
**Active Patterns**: NP-07, NP-13

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_signature_sha256` | command="echo hi"; expected_algorithm="sha256" | happy_path | Q1/AC-4.1 |
| 2 | `test_cached_replay_within_ttl` | ttl_seconds="60"; elapsed_seconds="10"; expected_cached_flag="true"; signature="abc123def456"; result="done" | happy_path | Q1/AC-4.2 |
| 3 | `test_cache_miss_normal_execution` | cache_state="miss"; expected_executed="true" | validation | Q2/AC-4.3 |
| 4 | `test_cache_miss_normal_execution` | cache_state="expired"; elapsed_seconds="120"; ttl_seconds="60"; expected_executed="true" | validation | Q2/AC-4.3 |
| 5 | `test_cache_concurrent_writes` | writer_count="4"; expected_valid_json="true"; state_mode="shared" | nfr_pattern | Q6/NP-13/AC-4.4 |
| 6 | `test_fr04_cache_unavailable_fallback` | cache_file_state="missing"; expected_executed="true" | nfr_pattern | Q6/1b/NP-07 (SAD: taskq.cache) — tests/integration/ |
| 7 | `test_fr04_cache_recovers_after_transient_outage` | cache_file_state="corrupted_then_restored"; expected_executed="true" | nfr_pattern | Q6/1b/NP-07 (SAD: taskq.cache) — tests/integration/ |
| 8 | `test_fr04_cache_actually_used_on_hit` | cache_state="hit"; expected_subprocess_spawned="false" | nfr_pattern | Q6/1b/NP-07 (SAD: taskq.cache) — tests/integration/ |
| 9 | `test_fr04_output_feeds_fr02_executor_pipeline` | cached_result_reused="true" | integration | Q7/FR-02 |

**Sub-assertions** (predicate over a case's declared Inputs / the production `result`):

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC41-algo | `expected_algorithm == "sha256"` | 1 |
| AC42-cached-flag | `expected_cached_flag == "true"` | 2 |
| AC43-executed | `expected_executed == "true"` | 3, 4, 6, 7 |
| AC44-concurrent-valid | `expected_valid_json == "true"` | 5 |
| AC45-hit-no-subprocess | `expected_subprocess_spawned == "false"` | 8 |
| AC46-pipeline | `cached_result_reused == "true"` | 9 |

**Properties** — round-trip invariant on cache put/get (Direction B):

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| P4-cache-roundtrip | `cache_get(cache_put(signature, result)) == result` | 2 |

`cache_get`/`cache_put` are symbolic (not bound to a declared Input), so the P2
engine degrades this to `needs_review`; the P4 `hypothesis @given` test
exercises the round trip across arbitrary signatures/results within TTL.

---

### FR-05: CLI integration

**Classification**: API_ENDPOINT, INTEGRATION
**Active Patterns**: NP-02, NP-04

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_subcommands_wired` | subcommands="submit,run,status,list,clear" | happy_path | Q1/Step2.5/AC-5.1 |
| 2 | `test_run_args` | run_flags="--cached,--all" | happy_path | Q1/AC-5.2 |
| 3 | `test_global_json_flag` | json_flag="true"; command_name="status" | happy_path | Q1/AC-5.3 |
| 4 | `test_exit_code_map` | outcome="success"; expected_exit="0" | happy_path | Q1/AC-5.4 |
| 5 | `test_exit_code_map` | outcome="validation_error"; expected_exit="2" | validation | Q2/AC-5.4 |
| 6 | `test_exit_code_map` | outcome="breaker_open"; expected_exit="3" | validation | Q2/AC-5.4 |
| 7 | `test_exit_code_map` | outcome="timeout"; expected_exit="4" | validation | Q2/AC-5.4 |
| 8 | `test_exit_code_map` | outcome="internal_error"; expected_exit="1" | validation | Q2/AC-5.4 |
| 9 | `test_status_list_clear` | filter_status="pending" | happy_path | Q1/AC-5.5 |
| 10 | `test_unknown_task_exit_2` | task_id="deadbeef"; expected_exit="2"; expected_stderr="unknown task: deadbeef" | validation | Q2/AC-5.6 |
| 11 | `test_corrupted_store_exit_1` | store_content="{invalid json"; expected_exit="1"; expected_stderr="store corrupted" | validation | Q2/AC-5.7 |
| 12 | `test_no_bare_except` | pattern="except:"; expected_matches="0" | negative_constraint | Q8/AC-5.8 |
| 13 | `test_inject_fault_rejected_in_production` | env="production"; cli_flag="--inject-fault=corrupt-mid-write"; expected_rejected="true" | nfr_pattern | Q6/1c/NP-02 (SEC:T-07) |

**Sub-assertions** (predicate over a case's declared Inputs / the production `result`):

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| AC51-subcommands | `"submit" in subcommands` | 1 |
| AC52-run-flags | `"--cached" in run_flags` | 2 |
| AC53-json | `json_flag == "true"` | 3 |
| AC54-exit0 | `expected_exit == "0"` | 4 |
| AC55-exit2 | `expected_exit == "2"` | 5, 10 |
| AC56-exit3 | `expected_exit == "3"` | 6 |
| AC57-exit4 | `expected_exit == "4"` | 7 |
| AC58-exit1 | `expected_exit == "1"` | 8, 11 |
| AC59-filter | `filter_status == "pending"` | 9 |
| AC510-unknown-stderr | `"unknown task" in expected_stderr` | 10 |
| AC511-corrupted-stderr | `"store corrupted" in expected_stderr` | 11 |
| AC512-no-bare-except | `expected_matches == "0"` | 12 |
| AC513-inject-rejected | `expected_rejected == "true"` | 13 |

---

## Cross-Cutting Test Cases

### NFR Integration

<!-- Integration-level NFRs (TEST_SPEC.md is the verifier): concrete Inputs +
     Sub-assertions declared per NFR, same shape as FR sections. Unit/Static
     NFRs are isolated in the Deferred table below instead. -->

### NFR-01: Performance — submit + status p95 < 50 ms

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_kpi_submit_status_p95_under_50ms_100_iter` | iterations="100"; sla_ms="50" | nfr_pattern | Q6/NP-06 |

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR01-sla | `sla_ms == "50"` | 1 |

### NFR-03: Reliability — atomic write + breaker recovery

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_atomic_write_three_files` | target_files="tasks.json,breaker.json,cache.json" | happy_path | Q1 |
| 2 | `test_mid_write_crash` | fault_type="mid_write_crash"; expected_outcome="valid_json_or_failfast" | fault_injection | Q5 |
| 3 | `test_recovery_within_cooldown_plus_1s` | cooldown_seconds="30"; expected_recovery_bound_seconds="31" | nfr_pattern | Q6/NP-15 |

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR03-files | `"tasks.json" in target_files` | 1 |
| NFR03-fault | `expected_outcome == "valid_json_or_failfast"` | 2 |
| NFR03-recovery | `expected_recovery_bound_seconds == "31"` | 3 |

### NFR-07: Resilience — fault injection (4 scenarios + production gating)

The `--inject-fault=<scenario>` CLI surface is gated by the test/development opt-in env var `TASKQ_INJECT_FAULT_OK=1` (per SAD.md §2.3.1). Cases 1–4 run with that env var set; case 5 confirms the production-rejection branch fires when the env var is unset.

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_corrupt_mid_write` | fault_type="corrupt-mid-write"; cli_flag="--inject-fault=corrupt-mid-write"; env="TASKQ_INJECT_FAULT_OK=1"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="true"; expected_outcome="recover_or_failfast" | fault_injection | Q5/NP-07 |
| 2 | `test_oserror_on_write` | fault_type="oserror-on-write"; cli_flag="--inject-fault=oserror-on-write"; env="TASKQ_INJECT_FAULT_OK=1"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="true"; expected_outcome="recover_or_failfast" | fault_injection | Q5/NP-07 |
| 3 | `test_disk_full` | fault_type="disk-full"; cli_flag="--inject-fault=disk-full"; env="TASKQ_INJECT_FAULT_OK=1"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="true"; expected_outcome="recover_or_failfast" | fault_injection | Q5/NP-07 |
| 4 | `test_kill_mid_write` | fault_type="kill-mid-write"; cli_flag="--inject-fault=kill-mid-write"; env="TASKQ_INJECT_FAULT_OK=1"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="true"; expected_outcome="recover_or_failfast" | fault_injection | Q5/NP-07 |
| 5 | `test_inject_fault_rejected_on_prod` | cli_flag="--inject-fault=corrupt-mid-write"; env="production"; env_TASKQ_INJECT_FAULT_OK="unset"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="false"; expected_rejected="true"; expected_exit="2"; expected_stderr="inject-fault rejected in production" | negative_constraint | Q8 |

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR07-outcome | `expected_outcome == "recover_or_failfast"` | 1, 2, 3, 4 |
| NFR07-scenario-in-flag | `fault_type in cli_flag` | 1, 2, 3, 4 |
| NFR07-test-gate-set | `env == "TASKQ_INJECT_FAULT_OK=1"` | 1, 2, 3, 4 |
| NFR07-rejected | `expected_rejected == "true"` | 5 |
| NFR07-rejected-exit2 | `expected_exit == "2"` | 5 |
| NFR07-rejected-stderr | `"inject-fault rejected in production" in expected_stderr` | 5 |

### NFR-08: Concurrency — cross-process safety

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_posix_flock` | platform="posix"; lock_api="fcntl.flock" | happy_path | Q1 |
| 2 | `test_network_fs_warning` | filesystem_type="nfs"; expected_warning="true" | validation | Q2 |
| 3 | `test_four_process_concurrent` | process_count="4"; subprocess_mode="out_of_process"; shared_TASKQ_HOME="true"; expected_valid_json="true" | nfr_pattern | Q6/1b/NP-13 (SAD: taskq.store) |

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR08-lockapi | `lock_api == "fcntl.flock"` | 1 |
| NFR08-warning | `expected_warning == "true"` | 2 |
| NFR08-valid | `expected_valid_json == "true"` | 3 |

### NFR-09: Scalability — 1000-task scale

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_kpi_submit_status_p95_under_100ms_1000_tasks` | task_scale="1000"; sla_ms="100" | nfr_pattern | Q6/NP-06 |
| 2 | `test_run_all_100_tasks_no_loss` | task_count="100"; expected_loss="0" | boundary | Q3 |
| 3 | `test_memory_under_100mb` | task_scale="1000"; memory_limit_mb="100" | nfr_pattern | Q6/NP-06 |

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR09-sla | `sla_ms == "100"` | 1 |
| NFR09-loss | `expected_loss == "0"` | 2 |
| NFR09-mem | `memory_limit_mb == "100"` | 3 |

### NFR-10: Evolvability — schema migration

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_version_field_invariant` | schema_version="1" | happy_path | Q1 |
| 2 | `test_v0_migrate_with_backup` | source_version="0"; expected_backup_suffix=".v0.bak"; expected_migrated_version="1" | state_transition | Q4/NP-11 |
| 3 | `test_v2_refuses` | source_version="2"; expected_exit="1"; expected_action="refuse" | validation | Q2 |
| 4 | `test_migration_fail_fast` | migration_outcome="failure"; expected_backup_retained="true"; expected_exit="1" | fault_injection | Q5 |

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| NFR10-version | `schema_version == "1"` | 1 |
| NFR10-migrate | `expected_migrated_version == "1"` | 2 |
| NFR10-refuse | `expected_action == "refuse"` | 3 |
| NFR10-failfast | `expected_backup_retained == "true"` | 4 |

## Cross-Cutting Test Cases (continued)

### Infrastructure & Middleware Integration

`taskq.cache` is a persistent-cache infrastructure component wired from the
single system entrypoint (`python -m taskq`). Isolated FR-04 unit tests are
insufficient by themselves — an E2E test starting from the CLI entrypoint
proves the wiring is real, not just unit-testable in isolation:

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_cli_entrypoint_wires_cache_end_to_end` | integration | Step 4 (infra wiring, FR-04 + FR-05) |

### Deferred to Downstream Phases

<!-- Unit/Static NFRs: TEST_SPEC.md is NOT the Integration-level verifier for
     these — their concrete test is already declared at unit/static layer
     inside an FR entry above (or is a project-wide static scan), so no
     separate Integration-level NFR Integration table is authored for them. -->

| # | Test Function | Type | Derivation |
|---|---|---|---|
| 1 | `test_shell_true_grep_zero_matches` | unit | NFR-02: repository-wide source scan confirms zero `shell=True` call sites (FR-02 case 1). |
| 2 | `test_submit_injection_semicolon_rejected` | unit | NFR-02: representative of the 7 dedicated injection-character negative unit tests (FR-01 cases 4-10); see FR-01 table for the other 6. |
| 3 | `test_sk_token_redacted` | unit | NFR-04: representative of the secret-redaction test set (`test_token_equals_redacted`, `test_sk_token_redacted_stderr`, `test_token_equals_redacted_stderr`, `test_secret_redaction_before_truncation`) covering both stdout/stderr streams and the truncation-boundary counterexample (FR-02 cases 11-15, SEC T-03); see FR-02 table for the others. |
| 4 | `test_docstring_fr_xx_tag_coverage` | static | NFR-05: every public function/class docstring carries an `[FR-XX]` tag. |
| 5 | `test_config_reads_all_8_env_vars_with_defaults` | unit | NFR-06: `config.py` exposes all 8 `TASKQ_*` readers with documented defaults. |
| 6 | `test_env_example_declares_all_8_vars` | static | NFR-06: `.env.example` declares all 8 `TASKQ_*` variables with a comment. |

### Deployment Smoke

`taskq` is a CLI tool with no HTTP server, so the canonical
`test_app_starts_and_health_endpoint_returns_200` smoke test is adapted to the
CLI equivalent — the module entrypoint must start and exit cleanly:

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_cli_entrypoint_starts_and_help_exits_0` | invocation="python -m taskq --help"; expected_exit="0" | smoke | deployment |

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| DEPLOY-help-exit0 | `expected_exit == "0"` | 1 |

---

## Summary

| Metric | Count |
|---|---|
| FRs covered | 5 / 5 |
| Total test cases | 86 |
| By type: happy_path | 14 |
| By type: validation/failure | 23 |
| By type: boundary | 5 |
| By type: state_transition | 8 |
| By type: fault_injection | 6 |
| By type: nfr_pattern | 23 |
| By type: integration | 3 |
| By type: negative_constraint | 3 |
| By type: smoke | 1 |
| Active NFR patterns applied | NP-02, NP-03, NP-04, NP-06, NP-07, NP-08, NP-09, NP-11, NP-13, NP-14, NP-15 |

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.9.1*
